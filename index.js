const page = document.getElementById('text');

window.addEventListener('DOMContentLoaded', () => {
    page.textContent = 'This is really cool!';
})

// page.addEventListener('change', () => {
//     page.textContent = 'This is really cool!'
// })

